# Apple Protocols

Project conventions, architecture and pitfalls are in [CLAUDE.md](CLAUDE.md). Read it first; it applies to every agent, not only Claude.

When something fails against real hardware, use the loop described there under "Debuggen tegen een echt device (agent-loop)": `bun run diag --help` lists the commands. The user starts the diagnostics app; never start it yourself.
