syntax = "proto2";

import "PlayerPath.proto";

message Destination {
    optional PlayerPath playerPath = 1;
    optional string outputContextUID = 2;
    optional string outputDeviceUID = 3;
    optional string endpoint = 4;
}
