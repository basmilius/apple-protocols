syntax = "proto2";

import "ProtocolMessage.proto";

extend ProtocolMessage {
    optional GetVolumeMutedResultMessage getVolumeMutedResultMessage = 99;
}

message GetVolumeMutedResultMessage {
    optional bool isMuted = 1;
}
