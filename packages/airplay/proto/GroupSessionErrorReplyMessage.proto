syntax = "proto2";

import "ProtocolMessage.proto";

extend ProtocolMessage {
    optional GroupSessionErrorReplyMessage groupSessionErrorReplyMessage = 110;
}

message GroupSessionErrorReplyMessage {
    optional string errorMessage = 1;
}
