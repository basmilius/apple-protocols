syntax = "proto2";

message LyricsItem {
    optional string lyrics = 1;
    optional bool userProvided = 2;
    optional LyricsToken token = 3;
}

message LyricsToken {
    optional string identifier = 1;
    optional bytes userData = 2;
}
