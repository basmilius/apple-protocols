syntax = "proto2";

import "LyricsEvent.proto";
import "PlayerPath.proto";
import "ProtocolMessage.proto";

extend ProtocolMessage {
    optional SendLyricsEventMessage sendLyricsEventMessage = 48;
}

message SendLyricsEventMessage {
    optional LyricsEvent event = 1;
    optional PlayerPath playerPath = 2;
}
