syntax = "proto2";

import "ProtocolMessage.proto";

extend ProtocolMessage {
    optional SetConversationDetectionEnabledMessage setConversationDetectionEnabledMessage = 90;
}

message SetConversationDetectionEnabledMessage {
    optional bool enabled = 1;
    optional string outputDeviceUID = 2;
}
