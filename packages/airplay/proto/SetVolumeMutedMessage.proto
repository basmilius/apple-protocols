syntax = "proto2";

import "ProtocolMessage.proto";

extend ProtocolMessage {
    optional SetVolumeMutedMessage setVolumeMutedMessage = 100;
}

message SetVolumeMutedMessage {
    optional bool isMuted = 1;
    optional string outputDeviceUID = 2;
}
