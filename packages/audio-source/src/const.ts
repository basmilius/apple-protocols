/** Default number of bytes per audio channel (16-bit). */
export const DEFAULT_BYTES_PER_CHANNEL = 2;

/** Default number of audio channels (stereo). */
export const DEFAULT_CHANNELS = 2;

/** Default audio sample rate in Hz (CD quality). */
export const DEFAULT_SAMPLE_RATE = 44100;

/** Number of audio frames per FFmpeg output packet, matching ALAC frame size. */
export const FFMPEG_FRAMES_PER_PACKET = 352;
